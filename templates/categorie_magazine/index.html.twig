{% extends 'base.html.twig' %}

{% block title %}CategorieMagazine index{% endblock %}

{% block body %}
    <h1>CategorieMagazine index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Designation</th>
                <th>Description</th>
                <th>Contenue</th>
                <th>Photo1024x768</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_magazine in categorie_magazines %}
            <tr>
                <td>{{ categorie_magazine.id }}</td>
                <td>{{ categorie_magazine.designation }}</td>
                <td>{{ categorie_magazine.description }}</td>
                <td>{{ categorie_magazine.contenue }}</td>
                <td>{{ categorie_magazine.photo1024x768 }}</td>
                <td>
                    <a href="{{ path('app_categorie_magazine_show', {'id': categorie_magazine.id}) }}">show</a>
                    <a href="{{ path('app_categorie_magazine_edit', {'id': categorie_magazine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_magazine_new') }}">Create new</a>
{% endblock %}
